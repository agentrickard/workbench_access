services:
  plugin.manager.workbench_access.scheme:
    class: Drupal\workbench_access\WorkbenchAccessManager
    arguments: ['@container.namespaces', '@cache.discovery', '@module_handler', '@entity_type.manager', '@state']
